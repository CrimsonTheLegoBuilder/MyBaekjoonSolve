# import tensorflow as tf
import math

print((83.205029**2 + 124.807544**2)**.5)
# height = [170, 180, 175, 160, 180]
# foot = [265, 280, 270, 260, 275]
#
# h = 170
# f = 265
# a = tf.Variable(0.1)
# b = tf.Variable(0.2)
#
#
# def loss_fn():
#     p = h * a + b
#     return tf.square(265 - p)
#
#
# opt = tf.keras.optimizers.Adam(learning_rate=0.1)
# for i in range(300):
#     opt.minimize(loss_fn, var_list=[a, b])
#     print(a.numpy(), b.numpy())



